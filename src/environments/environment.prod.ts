export const environment = {
  production: true,
  demo: 'default',
  apiUrl: 'https://backendoots.onrender.com/api/v1', // Cambiar por la URL de producción real
  apiTimeout: 30000,
  enableLogging: false,
  enableMockData: false,
  preferredCountries: ['co', 'us'], // Colombia and United States by default
};
